# Captured Claude Code payload contracts

Captured on 2026-07-11 from Claude Code against the local gateway. Session-specific fields (`session_id`, `transcript_path`, `prompt_id`, and `tool_use_id`) were removed. Workflow JavaScript is in `tool_input.script`; Agent routing is in `tool_input.subagent_type`.
